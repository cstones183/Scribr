# Scribr — macOS Voice-to-Text Menubar App
# Install with: pip install -r requirements.txt

# Global hotkey listener (tap-to-toggle detection)
pynput==1.7.6

# Microphone audio capture
sounddevice==0.4.6

# Audio processing
numpy==1.26.0
scipy==1.11.3

# Overlay UI (smooth 60fps animations on macOS)
# Pin the Qt runtime + sip to match the bindings. PyQt6==6.6.0 alone leaves
# PyQt6-Qt6 unpinned, so a clean install (e.g. CI) pulls the latest Qt6 and
# the ABI mismatch crashes the bundled app with a missing-symbol ImportError.
PyQt6==6.6.0
PyQt6-Qt6==6.6.3
PyQt6-sip==13.11.0

# macOS Accessibility API — detect focused text fields
pyobjc-framework-ApplicationServices
pyobjc-framework-Cocoa

# OpenAI — Whisper API transcription + GPT-4o-mini cleanup
openai==1.54.0

# HTTP client for Groq API (ultra-fast Whisper transcription)
requests==2.31.0

# Clipboard access
pyperclip==1.8.2

# App packaging (build distributable .app bundle)
pyinstaller==6.3.0
